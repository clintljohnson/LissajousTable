# LissajousTable-
A Lissajous table is a grid where each cell plots one oscillator's frequency against another's — the row index driving vertical motion, the column index horizontal — so every cell traces the Lissajous curve for that frequency ratio, with the reference oscillators shown as circles along the top edge and left edge.
